# Backend base URL. Leave empty ("") when served behind the Nginx proxy in Docker.
VITE_API_URL=http://localhost:8000

# Optional: preload sample lyrics for local development
VITE_DEBUG_LYRICS=false
VITE_DEBUG_LYRICS_TEXT=
